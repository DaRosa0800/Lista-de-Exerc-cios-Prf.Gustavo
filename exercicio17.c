#include <stdio.h>
#include <string.h>

void main(){

    char palavra[50];
    int a = 0;

    printf("Digite uma palavra");
    scanf("%c", palavra);

    for(int i = 0, palavra[1], i = "\0", i++);

}